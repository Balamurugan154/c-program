#include <stdio.h>

int main()
{
    int unit,bill;
    scanf("%d",&unit);
    bill=unit*5;
    
    if(unit<=100)
    
    printf("Bill amount = Rs.%d",bill);
    else
    printf("Bill amount = Rs.%d",bill);

    return 0;
}